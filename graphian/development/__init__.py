"""development ── 発達ルールの空き枠(§1 / §8)。

発達ルールの具体的な数学(報酬系の計算・確率分布・活動依存の発達則)は **本仕様では未確定**
であり、初版では実装しない。本パッケージが初版で確定するのは `base.py` の抽象基底
`DevelopmentRule`(契約)のみ。本物の数学は後続の「数学設計セッション」で、この契約を満たす
具象として差し替える(§6 / §8)。
"""
